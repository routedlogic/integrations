service: bbot-v2-logfile
input: logfile
policy_template: bbot
data_stream:
  vars:
    paths:
      - "{{SERVICE_LOGS_DIR}}/bbot-v2-logfile*.log"
    preserve_original_event: true
    tags:
      - forwarded
      - bbot
assert:
  hit_count: 125
